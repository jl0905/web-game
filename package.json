{
  "name": "web-game",
  "version": "1.0.0",
  "private": true,
  "description": "A raylib game compiled to WebAssembly, rendered on an HTML canvas",
  "scripts": {
    "build": "node build.js",
    "start": "node server.js",
    "dev": "node build.js && node server.js"
  }
}
